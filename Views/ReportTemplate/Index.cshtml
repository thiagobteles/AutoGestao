@model IEnumerable<AutoGestao.Entidades.Relatorio.ReportTemplateEntity>
@using AutoGestao.Entidades.Relatorio

@{
    ViewData["Title"] = "Templates de Relatórios";
}

<div class="container-fluid">
    <div class="page-header mb-4">
        <div class="d-flex justify-content-between align-items-center">
            <div>
                <h1 class="page-title">
                    <i class="fas fa-file-alt me-2"></i>
                    Templates de Relatórios
                </h1>
                <p class="text-muted">Gerencie templates personalizados para geração de relatórios</p>
            </div>
            <div>
                <a href="@Url.Action("Create", "ReportBuilder")" class="btn btn-lg" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); color: white; font-weight: 600;">
                    <i class="fas fa-magic me-2"></i>
                    Criar Novo Template
                </a>
            </div>
        </div>
    </div>

    <!-- Grid padrão será renderizada aqui -->
    <partial name="_StandardGrid" model="Model" />
</div>

<style>
    .btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(102, 126, 234, 0.4);
        transition: all 0.3s ease;
    }

    .page-header {
        background: white;
        padding: 25px;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.08);
    }

    .page-title {
        font-size: 28px;
        font-weight: 700;
        color: #2c3e50;
        margin-bottom: 5px;
    }
</style>

@section Scripts {
    <script>
        // Customizar ações da grid se necessário
        document.addEventListener('DOMContentLoaded', function() {
            console.log('📋 Templates de Relatórios carregados');

            // Adicionar ação customizada para editar no builder
            const editButtons = document.querySelectorAll('[data-action="edit"]');
            editButtons.forEach(btn => {
                btn.addEventListener('click', function(e) {
                    e.preventDefault();
                    const id = this.getAttribute('data-id');
                    window.location.href = `/ReportBuilder/Edit/${id}`;
                });
            });
        });
    </script>
}
